using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadMusicManager : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	    MusicManager.Instance.Exist();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
